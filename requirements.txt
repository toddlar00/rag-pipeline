# ============================================================
# RAG Pipeline — Requirements
# ============================================================
#
# IMPORTANT: Install PyTorch FIRST with CUDA 12.8 wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# Then:
#   pip install -r requirements.txt
#
# For optional features (Qdrant, Voyage AI, LLM classification, web UI):
#   pip install -r requirements.txt -r requirements-optional.txt
# ============================================================

# --- Core ---

# PDF preprocessing (strip Paper Capture background scans)
PyMuPDF>=1.24,<2

# PDF conversion + layout detection
docling>=2.114.0,<3
docling-core[chunking]>=2.87.1,<3
pypdfium2>=5.12.1,<6

# Embedding models (nomic, legal-bert, etc.)
sentence-transformers>=5.6.0,<6
# Reviewed Nomic remote-code module imports einops directly.
einops>=0.8.2,<1

# Vector store (default backend)
chromadb>=1.5.9,<2
# ONNX Runtime 1.24+ no longer publishes CPython 3.10 wheels. Keep the
# transitive Chroma dependency installable across the supported 3.10-3.14 range.
onnxruntime>=1.14.1,<1.24; python_version < "3.11"
onnxruntime>=1.27.0,<2; python_version >= "3.11"

# Cross-encoder reranker
FlagEmbedding>=1.4.0,<2

# BM25 keyword search (hybrid mode)
rank-bm25>=0.2,<0.3

# Progress bars
tqdm>=4.66,<5

# HTTP clients used by cloud embedding and LLM backends
requests>=2.31,<3

# Numerical operations used by RAPTOR clustering
numpy>=1.26,<3
