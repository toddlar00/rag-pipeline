# Main status at `443dce4`

- **Evidence ID:** `main-status-2026-08-18-443dce4`
- **Observed:** 2026-08-18
- **Repository state:** `main` and `origin/main` at
  `443dce4c312737eebb57a5bba6fc0abb9ace1a26`
- **Tree:** `344a9b224dd5942a50d370bc10dfb62384962440`
- **Commit subject:** `Freeze hardening-cycle Phase A0 evidence`
- **Evidence classes:** local technical self-audit; hosted workflow results;
  repository-history inspection
- **Privacy classification:** content-free aggregate and non-corpus repository
  workflow metadata; no private source content or derived evaluation wording
- **Supersedes:** none

This is a point-in-time record, not a release approval or owner review.

## Local technical baseline

On the review host with CPython 3.14.3:

- `python -m pytest -q`: 3,462 passed, 7 skipped, 658 warnings in 235.39
  seconds.
- Ruff passed.
- The tracked-source compile gate passed for 168 Python sources.
- The dependency, model-artifact, CI-security, and architecture gates passed.
- `python tools/check_architecture_inventory.py` validated the committed
  architecture inventory. Exact point-in-time inventory counts remain in that
  machine-readable file and the archived roadmap snapshot; live roadmap prose
  does not duplicate them.
- All three checked-in offline BM25 suites passed their committed baselines.

These results are agent-run technical evidence. They are not a submitted human
review and do not qualify a private corpus or a product release.

## Integrated convergence observed at the baseline

- [PR #44](https://github.com/toddlar00/rag-pipeline/pull/44) merged on
  2026-08-01 at validated head `ed2995e` (merge `2d8e4f9`).
- [PR #76](https://github.com/toddlar00/rag-pipeline/pull/76) merged at
  validated head `ddcbe89` (merge `471381c`).
- The first policy-compliant PDF/Docling domain update subsequently merged
  through [PR #83](https://github.com/toddlar00/rag-pipeline/pull/83).

These observations close the older R1/R2 convergence state. They do not close
the remaining dependency, vulnerability, license, qualification, packaging,
or release gates.

## Phase A0 identity and hosted result

The paired reports bind clean pre-gate source
`4551c50970a07ac120d192802bcc692e39e3ece6` (tree
`5267cf730ea1f85fa3455f77b4b40ac6f591db07`) to the following
reports-and-documentation-only evidence head, `443dce4`:

| Platform report | Runtime | Scenarios | Repetitions | Embedded report SHA-256 |
| --- | --- | ---: | ---: | --- |
| `benchmarks/phase-a0-windows-cpython312.json` | CPython 3.12.13 x86-64 | 9 | 5 | `a41545b403ed2d5f9dbea529ce86096550dd1114232c955b910b4f76336e496b` |
| `benchmarks/phase-a0-linux-cpython312.json` | CPython 3.12.13 x86-64 | 9 | 5 | `93ef091dd2828b24fcd2d875320a79ddd5435cb71e99ac97ca20b82a8ffb46c2` |

Both independent same-platform comparisons pass. The hosted
[CI run 32161574976](https://github.com/toddlar00/rag-pipeline/actions/runs/32161574976)
at exact evidence head `443dce4` succeeded, including both Windows and Linux
Phase A0 jobs. Retained A0 artifacts were present when audited and reported an
expiry of 2026-09-17.

This closes the previously pending hosted _technical_ checkpoint for this
source/evidence pair. No submitted exact-head human review or separate owner
authorization for the R8c-6 ownership move was found. The technical result
therefore must not be used as that authorization.

## Hosted dependency and security state

- [Dependency compatibility run 32161575026](https://github.com/toddlar00/rag-pipeline/actions/runs/32161575026)
  succeeded at `443dce4`.
- [Supply-chain security run 32161575011](https://github.com/toddlar00/rag-pipeline/actions/runs/32161575011)
  failed both audit jobs at `443dce4`.
- No branch protection or ruleset was present when audited. Until that changes,
  promotion needs an exact-SHA manual record rather than assuming a required
  check will prevent an unsafe merge.
- The failed Python audit reported unaccepted findings for `aiohttp`
  `PYSEC-2026-3545`, `cryptography` `PYSEC-2026-3552`, and `h2`
  `PYSEC-2026-3628`. The `chromadb` `PYSEC-2026-311` acceptance and the
  Torch/Torchvision audit skips were time-bounded through 2026-08-31.

The parked branch `agent/transitive-advisory-locks` was at `1138398`, with no
pull request or hosted checks. It was one commit ahead of merge base `70f92b`
and seven commits behind `main`; it changed four generated locks to select
`aiohttp` 3.14.3, `cryptography` 50.0.0, and `h2` 4.4.1. The maintained
dependency-domain policy rejects a transitive-only lock delta, so this record
does not authorize merging that branch.

## Limitations

- Live advisory feeds can change after this observation. The conclusions above
  bind to the named workflow runs and their scanner/advisory state, not to a
  timeless claim that those packages are clean or vulnerable.
- Ignored output directories and local private artifacts were not promoted to
  durable evidence.
- No private-source policy choice, corpus-owner judgment, dependency exception,
  license disposition, release version, or support tier was inferred.
